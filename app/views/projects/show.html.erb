<p id="notice"><%= notice %></p>

<div class="jumbotron" style="margin-top: 5rem; margin-bottom: -3rem;">
	<div class="container">
		<h1 class="jumbotron-heading">
			<%= @project.title %>
		</h1>
		<p>
			<%= @project.description %>
		</p>

		<p>
			<%= link_to 'Edit', edit_project_path(@project) %> |
			<%= link_to 'Back', projects_path %>
		</p>
	</div>
</div>

<% # See index.html.erb for comment
%>
<div>
<%= render partial: "shared/cards", collection: @projects %>
<div>